/**
 * SEG 3125 Lab4 Group6
 * File: main.js
 * @description Responsible for main functions in the page
 */

/**
 * @description Controller for miscellaneous components
 */
$(() => {
	// execute when document ready
	$(".carousel").carousel({
		interval: 4500, // Set the carousel interval to 3.5s
	});
});

$("book-btn").click(function (e) { 
	
});